require("module.autosave.config")
require("module.terminal.config")
require("module.diagPanel.Panel").setup({height = 8})
-- not needed to use run because F1 and F12 works fine
-- and are precise
-- require("module.run.config")
